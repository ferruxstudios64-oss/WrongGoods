import {notFound} from 'next/navigation';
import Link from 'next/link';
import {products} from '@/lib/catalog';
import {Purchase} from '@/components/store';
export function generateStaticParams(){return products.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);return {title:p?.name||'Collection not found',description:p?.description}}
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)notFound();return <main id="main" className="detail"><Link className="back" href="/#goods">← All goods</Link><div className="detail-grid"><div>{p.image?<img className="detail-image" src={p.image} alt={`${p.name} concept packaging preview`}/>:<div className={`detail-type ${p.slug}`}><span>{p.code}</span><h1>{p.name}</h1><span>CONCEPT IN DEVELOPMENT</span></div>}<p className="caption">Concept preview. Imagery illustrates the direction, not final downloadable contents.</p></div><div><span className="eyebrow">{p.code} / {p.tone}</span><h1>{p.name}</h1><p className="detail-intro">{p.description}</p><Purchase product={p}/><h2>Planned contents</h2><ul>{p.contents.map(c=><li key={c}>{c}</li>)}</ul><h2>File formats</h2><p>{p.formats.join(' / ')}</p><h2>Usage</h2><p>Intended for fictional worlds and creative productions. Product-specific rights will be shown before purchase.</p><Link className="text-link" href="/licence">Read the licensing approach ↗</Link></div></div></main>}
